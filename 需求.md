# 指标需求

## 功能名称：自定义个股池管理

目标：用户可以创建和管理多个自定义个股池，并在看盘面板中查看每个个股池的行情汇总信息。

### 一、个股池管理

1. 用户可以创建多个个股池。
2. 创建个股池时必须填写名称。
3. 用户可以对已有个股池进行以下操作：
   - 重命名；
   - 删除；
   - 添加股票；
   - 移除股票；
   - 查看池内股票明细。
4. 同一个个股池内不允许重复添加同一只股票。
5. 同一只股票可以同时存在于不同个股池。
6. 个股池名称不允许重复。
7. 不规定名称长度。
8. 不设置个股池数量和单个池内股票数量上限。

### 二、个股池卡片展示

每个个股池展示，需要包含：

1. 个股池名称
   - 支持重命名；
   - 名称过长时省略显示，鼠标悬停展示完整名称。
2. 个股池平均涨跌幅
   - 默认统计当前涨跌幅；
   - 计算方式：池内有效股票当前最新涨跌幅的算术平均值；
   - 展示为百分比，保留2位小数；
   - 正数显示红色，负数显示绿色，0显示黑色；
   - 排除停牌、无行情数据及退市股票。
3. 涨停股票数、跌停股票数、股票总数
   - 展示为三个独立的列，每列均可点击表头排序；
   - 涨停判断按股票所属板块适用对应涨跌幅限制：主板（含ST）10%，创业板/科创板 20%，北交所 30%；
   - 上市未满两周的股票涨跌停规则保守视为未知，不计入涨停/跌停数；
   - 停牌股票计入总数但不计入涨停数跌停数。
4. 资金净流入
   - 计算方式：池内全部有效股票资金净流入金额之和；
   - 必须明确统计周期，建议默认为“当前主力资金净流入”；
   - 正值显示红色，负值显示绿色，0显示中性色；
   - 金额应统一单位，例如自动转换为万元/亿元并保留2位小数；
   - 无数据股票不参与求和，但界面需要标识存在数据缺失；
   - 数据定义和数据来源必须固定，避免不同接口统计结果不一致。
5. 红盘率
   - 计算方式：池内当前涨幅大于0的股票个数➗池内有效股票个数（剔除停牌）
   - 展示为百分比，保留2位小数；

### 三、排序功能

1. 用户可以按照以下字段对个股池卡片排序：
   - 个股池名称；
   - 平均涨跌幅；
   - 涨停股票数量；
   - 跌停股票数量；
   - 股票总数；
   - 资金净流入；
   - 单位市值资金净流入；
   - 红盘率。
2. 每个字段支持升序和降序。
3. 需要明确默认排序规则，建议按创建时间倒序。
4. 当排序值相同时，建议按创建时间或名称进行二级排序。
5. 建议保存用户最后一次排序方式，重新进入页面后继续生效。
6. 需要明确排序作用于全部个股池，还是只作用于当前分组。

### 四、数据刷新

1. 交易时间内行情数据的刷新频率需要明确，例如每3秒、5秒或15秒刷新一次。
2. 非交易时间展示最近一个交易日的收盘数据。
3. 页面应展示“最后更新时间”。
4. 接口请求失败时：
   - 保留最近一次有效数据；
   - 显示数据更新失败或延迟提示；
   - 支持用户手动刷新。

### 五、空状态和异常状态

应提前定义以下情况：
例如，空个股池可以展示：

- 平均涨跌幅：--
- 涨停数量/跌停数量/总数：0/0/0
- 资金净流入：--
